# API origin only; use HTTPS in deployment. Blank means same-origin API routing.
# Used by both the browser API client and CSP connect-src at build time.
NEXT_PUBLIC_API_URL=http://localhost:8081
NEXT_PUBLIC_API_MODE=direct
